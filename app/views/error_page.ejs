<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body { background: #050505; color: #ff4d6d; font-family: monospace; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
        .box { border: 1px solid #ff4d6d; padding: 20px; box-shadow: 0 0 15px rgba(255, 77, 109, 0.5); text-align: center; }
        h1 { font-size: 1.2rem; margin-bottom: 10px; }
        p { color: #888; font-size: 0.9rem; }
    </style>
</head>
<body>
    <div class="box">
        <h1>[ SYSTEM ERROR ]</h1>
        <p><%= message %></p>
        <p style="margin-top: 20px; color: #444;">Trace ID: <%= Math.random().toString(36).substring(7) %></p>
    </div>
</body>
</html>